# GearTracr

## Getting Started
* `npm install webpack-dev-server webpack -g`
* `npm install`

To build GearTracr:
* `webpack`

To serve on [http://localhost:8080](http://localhost:8080)
* `webpack-dev-server`
